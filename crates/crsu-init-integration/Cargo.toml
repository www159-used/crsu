[package]
name = "crsu-init-integration"
version = "0.1.0"
edition = "2024"
description = "Init workflow integration tests for crsu"
license = "AGPL-3.0-or-later"
publish = false

[dev-dependencies]
crsu = { path = "../crsu", features = ["test-support"] }
crsu-testkit = { path = "../crsu-testkit" }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"

[lints]
workspace = true
