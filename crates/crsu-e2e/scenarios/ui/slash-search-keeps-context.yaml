name: slash search highlights a match without hiding nearby candidates
render:
  kind: search-results
  screen: project
  query: "11"
  items: 50
  terminal: { width: 100, height: 16 }
expect:
  visible_contains:
    - PROJECT-11
    - PROJECT-10
    - /11
